import axios from 'src/utils/axios';
import { Annotation } from 'src/constants';

const adminSubmitAnnotations = async (
  annotations: Annotation[],
  imageId: string
) => {};

export { adminSubmitAnnotations };
