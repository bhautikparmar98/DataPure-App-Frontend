//style
import { Variant } from '@mui/material/styles/createTypography';

export type BlockVariantProps = {
  font: {
    variant: Variant;
    label: string;
  };
};

export type Product = {
  id: number;
};
